name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '6.0.x'
      - name: Restore dependencies
        run: dotnet restore Nedev.FileConverters.DocToDocx.slnx
      - name: Build
        run: dotnet build Nedev.FileConverters.DocToDocx.slnx --no-restore --configuration Release
      - name: Run tests
        run: dotnet test src/Nedev.FileConverters.DocToDocx.Tests/Nedev.FileConverters.DocToDocx.Tests.csproj --no-build --configuration Release
